using UnityEngine;

/// <summary>
/// Test script để kiểm tra scripts có hoạt động không.
/// Xóa script này sau khi test xong.
/// </summary>
public class TestScript : MonoBehaviour
{
    void Start()
    {
        Debug.Log("TestScript is working!");
    }
}